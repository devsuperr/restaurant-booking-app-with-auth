# Restaurant Booking App With Auth

<p align="center">
  <img src="https://studiojungle.ai/logo.png" alt="Studio Jungle" height="60" />
</p>

> An AI-built application generated with Studio Jungle.

Built with [**Studio Jungle**](https://studiojungle.ai) — describe what you want, get a working app.

## Stack

- Supabase (Postgres + Auth + Edge Functions)

## Run locally

```bash
npm install
npm run dev
```

## Backend setup

This app uses Supabase for database, auth, and edge functions. To run locally:

1. Copy `.env.example` to `.env` and fill in your Supabase project URL + anon key
2. Run the migration: `supabase db push` (or paste `schema.sql` into the Supabase SQL editor)
3. Deploy edge functions: `supabase functions deploy`

## Deploy

The fastest path is via Studio Jungle's **Publish** button — one click deploys to a `*.studiojungle.dev` subdomain with HTTPS. You can also deploy this codebase to Vercel, Netlify, or AWS Amplify like any standard Vite project.

## Generated by Studio Jungle

This codebase was scaffolded by [Studio Jungle](https://studiojungle.ai), an AI-powered app builder. Open the project in Studio Jungle to keep iterating with chat — the AI can read every file, run real database queries, and ship features as you describe them.

[**→ Open in Studio Jungle**](https://studiojungle.ai)

---

Made with 🌿 in Studio Jungle.
